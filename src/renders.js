// ============================================================
// YOUR RENDER IMAGES
// ------------------------------------------------------------
// Put your render image files in this same /public folder, then
// list their exact filenames below (in the order you want them shown).
// The first one becomes the big hero image. The rest fill the gallery.
//
// Example: if your files are named shot1.png, shot2.png, etc:
//   export const RENDERS = ['shot1.png', 'shot2.png', 'shot3.png']
//
// You have ~10 images — just list whatever they're actually called.
// Filenames are case-sensitive and must match exactly.
// ============================================================

export const LOGO = 'forge_Logo.png'

export const RENDERS = [
  // <-- replace these with your real filenames, keep the quotes and commas
  'render1.png',
  'render2.png',
  'render3.png',
  'render4.png',
  'render5.png',
  'render6.png',
]

// The hero background render (big cinematic one). Defaults to the first render.
export const HERO_RENDER = RENDERS[0]
